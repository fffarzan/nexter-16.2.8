# Server Functions

An asynchronous function that runs on the server. It can be called them from the client through a network request (that's why they must be asynchronous).

A Server Action is an async function used with startTransition

# Refs

- [mutating data](https://nextjs.org/docs/app/getting-started/mutating-data)
